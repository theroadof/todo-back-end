server:
  port: 8800
spring:
  datasource:
    url: jdbc:h2:file:./h2/test
    driver-class-name: org.h2.Driver
  jpa:
    database: h2
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      path: /h2-console
      enabled: true